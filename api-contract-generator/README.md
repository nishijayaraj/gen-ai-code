User Prompt to invoke custom Agent

@workspace analyze backend and frontend and generate API contract and fix Angular models

## How to run

From the agent drop down choose api-contract-agent.md

Then provide the user prompt "use api-contract-architect to generate api contract and fix angular models"
